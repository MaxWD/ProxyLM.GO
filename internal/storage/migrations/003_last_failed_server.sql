-- v0.8: добавлена колонка last_failed_server — имя сервера, на котором
-- предыдущая попытка этого запроса упала. Пустая строка означает «ни одной
-- неудачной попытки не было». Используется TUI для отрисовки красного
-- крестика в колонке Server рядом с текущим назначенным сервером.
--
-- Backfill: для старых строк значение '' — безопасный дефолт (история до
-- v0.8 информацию об упавших попытках не сохраняла; UI просто не подсветит
-- крестик для уже завершённых записей).

ALTER TABLE requests ADD COLUMN last_failed_server TEXT NOT NULL DEFAULT '';
