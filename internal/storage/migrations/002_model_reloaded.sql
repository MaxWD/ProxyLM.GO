-- v0.7: добавлена колонка model_reloaded — флаг «сервер переключал current_model
-- в начале этой задачи». Используется UI (колонка RM) и регрессией perf для
-- отделения «холодных» (с временем загрузки модели) вызовов от «горячих».
--
-- Backfill: для старых строк значение 0 — безопасный дефолт, влияющий только
-- на отображение/статистику (мы просто не знаем, грузилась ли модель).

ALTER TABLE requests ADD COLUMN model_reloaded INTEGER NOT NULL DEFAULT 0;
